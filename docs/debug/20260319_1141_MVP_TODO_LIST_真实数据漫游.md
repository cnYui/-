# MVP TODO LIST：真实数据漫游

## 📅 创建时间
2026年3月19日 11:41

## 使用说明
- `P0`：必须完成，直接影响主流程可用性
- `P1`：建议完成，提升稳定性与可用性
- `P2`：可延后，优化体验

---

## Phase 0：数据准备（P0）

### T0-1 真实帖子数据入库规范
- [x] 定义入库字段映射（`note_id/title/author/tags/image_count/image_files/city/location_name/content/lat/lng`）
- [x] 增加基础清洗规则（空值、脏标签、重复帖子）
- [x] 输出一份导入样例（使用当前小红书20条数据）
- 验收：数据库可查询到20条城市帖子，字段完整率 >= 95%

### T0-2 PostgreSQL表设计与迁移（小红书多图）
- [x] 设计 `posts` 扩展字段：`source_platform/source_note_id/title/content/tags/mood/city/district/location_name/lat/lng/is_public/created_at`
- [x] 设计 `post_images` 子表：`id/post_id/image_index/image_url/original_path/width/height`
- [x] 执行 migration 到 PostgreSQL，并保证可重复执行
- 验收：`posts + post_images` 建表完成，迁移脚本可重复执行不报错

### T0-3 小红书CSV字段映射到当前发帖字段
- [x] 将 `note_id` 映射到 `source_note_id`
- [x] 将 `title + tags` 组合映射到 `content/tags`
- [x] 将 `image_files(;)` 拆分写入 `post_images`
- [x] 对齐当前发帖字段：`content/imageUrl(multi)/mood/city/district/locationName/lat/lng`
- [x] 对 `image_files` 按数字序排序（避免 `0,1,10,2...`）
- 验收：20条帖子全部可落库，9条多图帖子图片数量与源数据一致

### T0-4 表情与地理位置补全
- [x] 根据 `tags/title/content` 为每条帖子生成 `mood`（表情）
- [x] 基于内容规则补 `city/location_name`，并补全 `lat/lng`
- [x] 无法精确定位时回退城市中心坐标（记录 `geo_confidence`）
- 验收：20条帖子 `mood` 覆盖率 100%，`lat/lng` 可用率 100%

### T0-5 城市候选池准备
- [x] 按城市聚合帖子
- [x] 给每条帖子补“粗分类标签”（吃/逛/玩）
- 验收：目标城市可返回足够候选点（建议 >= 30 条）

### T0-6 地图展示联调（真实多图帖子）
- [x] 后端地图接口返回真实入库帖子（含 `mood`、主图、坐标）
- [x] 前端地图可展示小红书导入帖子的表情点位
- [x] 点击点位可打开卡片，卡片包含标题/文案/主图
- 验收：地图上可见20条导入帖的点位，点击展示正常

## Phase 1：前端入口改造（P0）

### T1-1 文案与交互改造
- [x] `开始旅行` -> `开始漫游`
- [x] 弹窗标题/按钮文案同步更新
- [x] 保持原有出发地/目的地联动逻辑
- 验收：用户可正常打开弹窗并提交

### T1-2 启动接口对接
- [x] `travel-manager` 启动流程改为真实数据计划接口
- [x] 兼容现有卡片结构（`estimatedDays + dailyPlans`）
- 验收：点击开始后卡片可渲染，不卡死

---

## Phase 2：后端漫游计划编排（P0）

### T2-1 计划生成逻辑替换
- [x] 从“AI生计划”改为“真实帖子候选池编排”
- [x] 实现按天分组（Day1~DayN）
- [x] 支持基础加权（热度/标签多样性/距离）
- 验收：返回结构与前端兼容，空计划率为 0

### T2-2 进度推进复用
- [x] 复用现有 `travel_progress` 自动推进
- [x] 跨天节点触发“每日总结任务”
- 验收：可从 Day1 推进到结束，无重复推进

### T2-3 兜底策略
- [x] 城市帖子不足时回退模板点位
- [x] 候选池异常时返回可运行最小计划
- 验收：无 500 中断主流程

---

## Phase 3：每日总结与邮件（P0）

### T3-1 每日单次模型调用
- [x] 每日结束后聚合“当日多个景点”
- [x] 调用一次 LLM 生成总结 JSON
- [x] JSON字段：推荐项、不推荐项、理由、次日建议
- 验收：每个自然日仅 1 次调用，日志可追踪

### T3-2 邮件入库与展示
- [x] 写入 `mails`（含 `extra_data`）
- [x] 收信箱展示推荐摘要
- 验收：每日都有一封可读邮件，不重复发送

### T3-3 模型失败兜底
- [x] 超时/报错时使用模板内容发信
- 验收：即使 LLM 失败也有邮件产出

---

## Phase 4：稳定性与验收（P1）

### T4-1 地图基础性能保护
- [x] 限制一次加载最大点位数（例如 200）
- [x] 地图移动触发请求做防抖
- 验收：中端手机上拖动地图无明显卡顿

### T4-2 任务幂等与防重
- [x] 每日总结任务增加幂等键（`planId + day`）
- [x] 防止重复发信
- 验收：重复轮询不产生重复邮件

### T4-3 端到端测试清单
- [x] 启动漫游
- [x] 推进跨天
- [x] 邮件生成
- [x] 漫游完成回收状态
- 验收：主链路 4/4 全通过

---

## Phase 5：MVP后续（向量数据库，P2）

### T5-1 数据预处理（先做）
- [ ] 帖子去重与地点归一
- [ ] 标签标准化
- [ ] 评论摘要化

### T5-2 向量化与检索
- [ ] 帖子向量化
- [ ] 用户记忆向量化
- [ ] 相似召回接口

### T5-3 与每日总结融合
- [ ] 每日总结先向量召回候选，再交给 LLM 解释
- 验收：推荐相关性提升且可解释

---

## 里程碑定义

### M1（可演示）
- 完成 Phase 0~2
- 能看到“开始漫游 -> 卡片推进”

### M2（可用）
- 完成 Phase 3
- 每日邮件闭环完成

### M3（稳定）
- 完成 Phase 4
- 具备可持续迭代基础
